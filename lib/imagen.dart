import 'package:flutter/material.dart';

class Imagen_producto_carga extends StatelessWidget {
  const Imagen_producto_carga({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
